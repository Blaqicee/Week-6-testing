import { render, screen } from '@testing-library/react';
import BugForm from './BugForm';

test('renders form inputs', () => {
  render(<BugForm onAdd={() => {}} />);
  expect(screen.getByPlaceholderText(/Title/i)).toBeInTheDocument();
});
